#include "func.h"

int main()
{
    printf("what is your name? \n");
    scanf("%s",name);
    getchar();

    while(1)
    {
        if(!strcmp("bye",in))
        {
            printf("bye\n\n");
            break;
        }
        if(!strcmp(buffer,""))
        {
            return_answer(output);
        }
        if(buffer = NULL)
        {
            printf("i don't know. please teach me.\n\n");
            TEACH();
        }
    }
}
